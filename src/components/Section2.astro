<!-- Section 2: Top rated green spaces -->
<section class="top-rated-section">
  <div class="top-rated-header">
    <p>Top rated green spaces in Lisbon</p>
  </div>

  <div class="top-rated-grid">
    <div class="place-card">
      <img src="/images/1.png" alt="Jardim Mário Soares" />
      <div class="place-info">
        <span class="number">01.<span class="dot"></span></span>
        <p>Jardim Mário Soares</p>
        <img src="/images/pin.svg" class="leaf-icon" />
      </div>
    </div>

     <div class="place-card">
      <img src="/images/3.png" alt="Jardim da Fundação Calouste" />
      <div class="place-info">
        <span class="number">02.<span class="dot"></span></span>
        <p>Jardim da Fundação Calouste</p>
        <img src="/images/pin.svg" class="leaf-icon" />
      </div>
    </div>

    <div class="place-card">
      <img src="/images/2.png" alt="Parque Eduardo VII" />
      <div class="place-info">
        <span class="number">03.<span class="dot"></span></span>
        <p>Parque Eduardo VII</p>
        <img src="/images/pin.svg" class="leaf-icon" />
      </div>
    </div>

    <div class="place-card">
      <img src="/images/5.png" alt="Parque da Bela Vista" />
      <div class="place-info">
        <span class="number">04.<span class="dot"></span></span>
        <p>Parque da Bela Vista</p>
        <img src="/images/pin.svg" class="leaf-icon" />
      </div>
    </div>
  </div>

  <div class="see-all-wrapper">
    <a class="see-all" href="/map#carousel" aria-label="Se alle — gå til karusel på kort-siden">
      <span>See all</span>
      <img src="/images/touch.svg" class="fingerprint" alt="cta" />
    </a>
  </div>
</section>

<style>
.top-rated-section {
  position: relative;
  margin: 190px;
  box-sizing: border-box;
}

/* full-bleed 1px line */
.top-rated-section::before {
  content: "";
  position: absolute;
  left: 50%;
  transform: translateX(-50%);
  top: 0;
  width: 100vw;
  max-width: none;
  height: 1px;
  background-color: #d8dccf;
  z-index: 0;
}

.top-rated-header p {
  font-size: 16px;
  font-weight: bold;
  width: 100%;
  margin-bottom: 2rem;
}

.top-rated-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 1.5rem;
}

.place-card {
  overflow: hidden;
  display: flex;
  flex-direction: column;
  background: transparent;
  height: 100%;
}

.place-card img {
  width: 100%;
  height: autp;
  display: block;
  transition: transform 260ms cubic-bezier(.2,.8,.2,1);
  transform-origin: center center;
  will-change: transform;
}

.place-card:hover img,
.place-card:focus-within img {
  transform: scale(1.06);
}

.place-info {
  display: flex;
  align-items: center;
  gap: 0.35rem;
  font-size: 16px;
  padding: 12px 8px;
}

.number {
  font-weight: 700;
  font-size: 40px;
  line-height: 1;
  margin-right: 8px;
}

.leaf-icon {
  width: 40px !important;
  height: auto !important;
  margin-left: auto;
  max-width: none !important;
}

.see-all-wrapper {
  margin-top: 5rem;
  display: flex;
  justify-content: flex-end;
  align-items: center;
}

.see-all {
  font-size: 56px;
  font-weight: 600;
  color: #0c2f0c;
  text-decoration: none;
  display: inline-flex;
  align-items: center;
  gap: 1rem;
}

/* ensure the text inside the CTA also animates on hover */
.see-all > span {
  display: inline-block; /* allow transform on inline text */
  transition: transform 160ms ease;
}

.see-all:hover > span {
  transform: translateX(8px);
}

.fingerprint {
  width: 140px;
  height: auto;
  transition: transform 160ms ease;
  display: block;
}

.see-all:hover .fingerprint {
  transform: translateX(8px);
}

/* ---------- Responsive rules (only below) ---------- */

/* medium screens: 2 columns */
@media (max-width: 1100px) {
  .top-rated-section { margin: 120px 28px; }
  .top-rated-grid { grid-template-columns: repeat(2, 1fr); gap: 1rem; }
  .place-card img { height: 200px; }
  .number { font-size: 36px; }
  .see-all { font-size: 40px; }
  .fingerprint { width: 110px; }
}

/* small tablets / large phones: compact */
@media (max-width: 768px) {
  .top-rated-section { margin: 80px 20px; }
  .top-rated-header p { font-size: 14px; margin-bottom: 1rem; }
  .top-rated-grid { grid-template-columns: repeat(2, 1fr); gap: 12px; }
  .place-card img { height: 160px; }
  .number { font-size: 32px; }
  .see-all-wrapper { margin-top: 2.5rem; justify-content: start; }
  .see-all { font-size: 28px; gap: 0.6rem; }
  .fingerprint { width: 64px; }

  /* leaf icon smaller on these widths */
  .leaf-icon {
    width: 25px !important;
    height: 100% !important;
    margin-left: auto;
    max-width: none !important;
  }
}

/* mobile: single column stacked, keep same visual identity */
@media (max-width: 480px) {
  .top-rated-section {
    margin: 48px 16px;
  }
  .top-rated-header p {
    font-size: 14px;
    text-align: left;
    margin-bottom: 12px;
  }

  .top-rated-grid {
    grid-template-columns: 1fr;
    gap: 12px;
  }

  .place-card {
    flex-direction: row;
    gap: 12px;
    align-items: center;
  }

  .place-card img {
    width: 40%;
    height: 110px;
    flex-shrink: 0;
    border-radius: 8px;
  }

  .place-info {
    padding: 8px 0;
    gap: 8px;
    align-items: flex-start;
    flex-direction: column;
    text-align: left;
  }

  .number {
    font-size: 28px;
    display: block;
  }

  .place-info p {
    margin: 0;
    font-size: 16px;
    font-weight: 600;
    color: inherit;
  }

  .leaf-icon {
    width: 20px !important;
    margin-left: 0;
    align-self: flex-end;
  }

  /* See all centered and reduced */
  .see-all-wrapper {
    margin-top: 1.5rem;
    justify-content: center;
  }
  .see-all {
    font-size: 20px;
    gap: 0.5rem;
  }
  .fingerprint { width: 44px; }
}
</style>
